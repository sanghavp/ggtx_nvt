# Context Handoff

**Feature:** Update Logo Toàn Dự Án
**Plan file:** .agent/plans/PLAN_UPDATE_LOGO.md

## 🔗 Context Chain
| # | Workflow | Ngày | Kết quả | Ghi chú |
|---|----------|------|---------|--------|
| 1 | /create-plan | 2026-03-29 | ✅ Plan created | Đã xác nhận đổi màu CSS và làm trong suốt nền ảnh. |
| 2 | /review-plan | 2026-03-29 | ✅ PASS | Đã check path Favicon đúng chuẩn App Router (`src/app/`) và tên CSS Variables (`--color-primary`). |

## 📁 Files đã phân tích / Impact
- `public/images/newLogo.jpg` — File logo gốc do user tải lên (jpg trắng).
- `public/images/logo.png` — Nơi cần render ra logo mới.
- `src/app/favicon.ico` — Favicon cần overwrite (Next 15 App router).
- `src/styles/variables.css` — Nơi định nghĩa màu chờ được thay theo brand color mới (`--color-primary...`).
- Các component `HeroBanner.tsx`, `Header.tsx`, `login/page.tsx` — Chờ gắn query cache `?v=2` để reset tránh browser hard-cache.

## 📌 Decisions đã chốt
- Xoá nền chuyển `.jpg` thành `.png` trong suốt bằng Tool tự động (VD script Python/Nodejs local).
- Trích xuất tự động màu dominant, thay thế lại hệ Theme Color con CSS `--color-primary`.
- Resize tạo thư viện Favicon `src/app/favicon.ico`.

## ➡️ Next
- Workflow IDE Agent tiếp theo nên gọi lệnh chạy: `/implement-plan .agent/plans/PLAN_UPDATE_LOGO.md` 
