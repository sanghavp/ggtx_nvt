# Context Handoff

**Feature:** Fix QA Bugs Admin
**Plan file:** .agent/plans/PLAN_FIX_QA_BUGS.md

## 🔗 Context Chain

| # | Workflow | Kết quả | Ghi chú |
|---|----------|---------|--------|
| 1 | `/test` | ✅ Hoàn thành Test | Tìm ra 3 Bug (Mobile menu, Xoá News, Modal confirm) |
| 2 | `/create-plan` | ✅ Plan created | Đã lên kế hoạch fix lỗi trong file `PLAN_FIX_QA_BUGS.md` |

## 📁 Files đã phân tích

- `src/components/admin/Sidebar.tsx` — Chứa UI sidebar nhưng thiếu logic responsive toggle menu.
- `src/components/admin/AdminHeader.tsx` — Nơi cần gắn nút Hamburger.
- `src/app/admin/(dashboard)/layout.tsx` — Layout chính bọc Admin.
- `src/components/admin/NewsForm.tsx` — Hoàn toàn thiếu nút Xóa ở Action bars.
- `src/app/api/admin/news/[id]/route.ts` — DELETE route hoạt động tốt (Đã có sẵn logic).
- `src/components/admin/MilestoneForm.tsx` — Giao diện đang dùng `window.confirm` lỗi thời.

## 📌 Decisions đã chốt

- **Phương pháp Fix BUG-001**: Xây dựng 1 Context React (`SidebarContext`) cho toàn bộ layout Admin dashboard để đóng mở Drawer trên Mobile/Tablet. 
- **Cách can thiệp Server Component**: Vì Header có load Session Auth, nên việc thêm Button OnClick đóng/mở sidebar sẽ làm bằng 1 Client Component rỗng có tên `<MobileMenuToggle />` nhúng vào `<AdminHeader />`.
- **Phương pháp Fix BUG-002 & BUG-003**: Tạo custom component `<ConfirmModal />` để làm hộp thoại xác nhận chuyên nghiệp, tái sử dụng tại cả Form Tin tức và Cột mốc. 

## ➡️ Next

- Chờ User xác nhận quyết định qua các Open Questions trong Artifact Implementation Plan.
- Workflow tiếp theo nên chạy: `/implement-plan` để IDE Agent thực thi mã nguồn xử lý triệt để 3 vấn đề trên theo Plan.
